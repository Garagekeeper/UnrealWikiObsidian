
# 1.  지난시간 실습풀이
## MazeActor를 통한 Maze생성


- 반환값은 복사가 아닌지 : 컴파일러단에서 최적화해줌 [[NRVO]] 
-  MakeUnique도 복사해서 주는게 아닌지 : 이동 대입 연산자
-  유니크 포인터를 매개변수로 넘겨도 되는지 : 소유권이 이전되지 않는다는 점에 유의하고 사용하자
	- 소유권은 객체의 메모리를 해제할 유일한 권한을 의미 하는듯.
```cpp Title:UniquePtr_Factory
TUniquePtr<FMazeData> AMazeActor::MakeMazeData()
{
	TUniquePtr<FMazeData> Maze = MakeUnique<FMazeData>();
	Maze->MakeMaze(Width, Height, Seed);

	return Maze;
}
void AMazeActor::SpawnCells(FMazeData* InMaze)
{
	if (!CellUnitActor) return;
	SpawnedCellActors.Reserve(Width * Height);
}



```

런타임에 NavMesh를 설정하기 위해서 해당 옵션을 수정
![[2026.08.12 Unreal C++ 수업-1786498073602.webp]]
- Static : 런타임 수정 불가
- Dynamic : 런타임 수정 가능
- Dynamic Mod only: 가중치 수정만 런타임 반영

그런데 현재 방식은 런타임에 CellActor를 하나씩 스폰 하는데 이때마다 네브매시 수정하면 낭비니까는 끝나면 한번에 하자

```cpp file:MazeActor.cpp
void AMazeActor::OnPreMazeGenerate()
{

	//월드상에 있는 네비게이션 시스템을 받아옴
	// NavMesh 자동 업데이트 막기
	UNavigationSystemV1* NavSystem = FNavigationSystem::GetCurrent<UNavigationSystemV1>(GetWorld());
	if (NavSystem)
	{
		UNavigationSystemV1::SetNavigationAutoUpdateEnabled(false, NavSystem);
	}
}

void AMazeActor::OnPostMazeGenerate()
{

	//월드상에 있는 네비게이션 시스템을 받아옴
	// NavMesh 자동 업데이트 재개
	UNavigationSystemV1* NavSystem = FNavigationSystem::GetCurrent<UNavigationSystemV1>(GetWorld());
	if (NavSystem)
	{
		NavSystem->Build();
		UNavigationSystemV1::SetNavigationAutoUpdateEnabled(true, NavSystem);
	}
}
```

지금 구조는 Cell Actor가 너무 많은 매시를 들고 있다.
이를 [[ISM]]을 통해서 해결 근데 이거는 안보이는 것도 다 그려버림, 그래서 조금 더 나아간게
[[HISM]] 인데 이건 안보이는건 안그린다.
```
상세 비교 (HISM vs ISM)

1. ISM (Instanced Static Mesh)

- **작동 방식**: 모든 인스턴스를 하나의 거대한 메쉬처럼 묶어 렌더링합니다.
- **LOD 적용**: 하나의 인스턴스만 멀어져도 전체 그룹의 LOD가 변하지 않고, 그룹 전체가 멀어져야 LOD가 바뀝니다.
- **컬링 방식**: 화면에 인스턴스가 단 하나라도 보이면 그룹 전체를 렌더링합니다.
- **추천 대상**: 좁은 구역에 모여 있는 동일 오브젝트(예: 체스판의 말, 조립식 건물의 벽면 패널).

2. HISM (Hierarchical Instanced Static Mesh)

- **작동 방식**: 인스턴스들을 트리(Tree) 구조의 계층으로 나누어 관리합니다.
- **LOD 적용**: 카메라 거리에 따라 각 인스턴스가 개별적으로 LOD 단계를 전환합니다.
- **컬링 방식**: 화면 밖에 있거나 멀리 있는 인스턴스는 개별적으로 화면에서 지워집니다(오클루전 컬링).
- **추천 대상**: 넓은 맵에 흩어져 있는 자연물(예: 거대한 숲의 나무, 잔디, 넓게 퍼진 바위).

선택 가이드

- 오브젝트가 **좁은 지역**에 모여 있고 **LOD가 필요 없다면** ➡️ **ISM**이 메모리 효율상 유리합니다.
- 오브젝트가 **넓은 지역**에 퍼져 있고 **LOD와 거리 컬링이 필수라면** ➡️ **HISM**을 선택해야 합니다.

구현하시려는 콘텐츠의 특징을 알려주시면 어떤 컴포넌트가 더 적합할지 추천해 드릴 수 있습니다.

- 배치하려는 **오브젝트의 종류**(예: 잔디, 건물, 군중 애니메이션 등)
- 대략적인 **배치 수량**(예: 수백 개, 수만 개 이상)
```

```cpp Title:Using_HISM
	/*--------------------------
	*  HISM
	---------------------------*/
	UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "Maze|Components")
	TObjectPtr<UHierarchicalInstancedStaticMeshComponent> FloorHISM = nullptr;

	UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "Maze|Components")
	TObjectPtr<UHierarchicalInstancedStaticMeshComponent> WallHISM = nullptr;

	UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "Maze|Components")
	TObjectPtr<UHierarchicalInstancedStaticMeshComponent> GateHISM = nullptr;
	
	
for (uint8 y = 0; y < Height; y++)
{
	for (uint8 x = 0; x < Width; x++)
	{
		FCellData* Cell = InMaze->GetCell(x, y);
		if (!Cell) continue;

		FVector CellLocation = StartLoc + FVector(-y * CellSize, x * CellSize, 0.0f);
		MakeCellHISM(Cell, CellLocation);
	}
}

void AMazeActor::MakeCellHISM(const FCellData* InCell, const FVector& InLocation)
{
	if (!InCell) return;


	// 바닥 추가
	FloorHISM->AddInstance(FTransform(FRotator::ZeroRotator, InLocation));

	//벽 추가
	FVector Locattion;
	FRotator Rot;

	Locattion = InLocation + FVector::ForwardVector * CellHalfSize;
	Rot = FRotator::ZeroRotator;
	WallHISM->AddInstance(FTransform(Rot, Locattion));
	// 문 추가
	if (InCell->IsWall(EDirType::North))
	{
		GateHISM->AddInstance(FTransform(Rot, Locattion));
	}

	Locattion = InLocation + FVector::LeftVector * CellHalfSize;
	Rot = FRotator(0.0f, -90.0f, 0.0f);
	WallHISM->AddInstance(FTransform(Rot, Locattion));
	// 문 추가
	if (InCell->IsWall(EDirType::West))
	{
		GateHISM->AddInstance(FTransform(Rot, Locattion));
	}

	if (InCell->PosX == Width - 1)
	{
		Locattion = InLocation + FVector::RightVector * CellHalfSize;
		Rot = FRotator(0.0f, 90.0f, 0.0f);
		WallHISM->AddInstance(FTransform(Rot, Locattion));
		GateHISM->AddInstance(FTransform(Rot, Locattion));	// 동쪽 끝은 막혀 있는게 확정
	}
	if (InCell->PosY == Height - 1)
	{
		Locattion = InLocation + FVector::BackwardVector * CellHalfSize;
		Rot = FRotator(0.0f, 180.0f, 0.0f);
		WallHISM->AddInstance(FTransform(Rot, Locattion));
		GateHISM->AddInstance(FTransform(Rot, Locattion)); // 남쪽 끝은 막혀 있는게 확정
	}
```


# 3 기타 팁
- NavMesh경우는 기본적으로 런타임에 수정이 안됨
- Draw call batching, 같은 머터리얼을 사용하는 스태틱매시는 한번의 DrawCall
	- 사실 이 용어는 Unity에서 많이 쓰는 것 같고
	- GPU Instancing을 위해서 Dynamic Instancing을 지원하는 것인데. 의미는 동일한 것 같다.

# 4. 학습할 키워드
- [ ] [[NRVO]]
- [x] [[HISM]]
- [x] [[ISM]]

