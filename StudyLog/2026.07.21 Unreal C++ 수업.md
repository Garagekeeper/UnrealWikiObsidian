# 오늘 배운 내용
## 1. 지난 시간 내용
- CreateDefaultSubobject로 컴포넌트 만들면 자동으로 리플랙션 시스템에 등록(확인해볼것)
- 파이 값
	- PI(Deprecate), UE_PI
- UE_LOG의 TEXT()는 printf처럼 사용하면 된다

```cpp
UE_LOG(LogTemp, Log, TEXT("ElapsedTimmd : %.2f", ElapsedTime));
``` 

- 그냥 포인터 쓰기보다는 TobjectPtr 사용하자
	- 강한 참조임을 알고 사용하자
	- 잠깐 쓰이는거면 weak로 사용하자

- `FMath::Fmod` : float용 모듈로 연산

## 2. 입력 처리 
- 매핑 컨텍스트는 컨트롤러
- 바인딩은 캐릭터
- `SetupPlayerInputComponent`에서 입력 처리 권장
	- 타이밍 조절이 되어있음
![[Pasted image 20260721111654.png]]

- IMC 코드에서 쓰려면 Build.cs를 수정해야함 (길찾기, UI, 나이아가라도 마찬가지)
- ![[Pasted image 20260721112149.png]]
	- 그리고 사용할 헤더를 수정(전방선언)
```cpp
	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly)
	TObjectPtr<class UInputMappingContext> DefaultMappingContext = nullptr;
```

- 그리고 에디터도 새로고침(솔루션 제네레이트 다시하거나, 에디터에서 새로고침 하던가)
	- 파일을 추가 제거하면 반드시 새로고침을 해
```cpp
// 헤더도 중요합니다.
#include "ActionPlayerController.h"
#include "EnhancedInputSubsystems.h"
#include "InputMappingContext.h"

void AActionPlayerController::BeginPlay()
{
	Super::BeginPlay();

	UEnhancedInputLocalPlayerSubsystem* SubSystem =
		ULocalPlayer::GetSubsystem<UEnhancedInputLocalPlayerSubsystem>(GetLocalPlayer());

	if (SubSystem && DefaultMappingContext)
	{
		SubSystem->AddMappingContext(DefaultMappingContext, GameInputPriority);
	}
}
```


컨트롤러에서 매핑을 시키고
캐릭터에서 액션을 넣고
	인풋액션 선언하고
		액션이 어떤 함수를 실행할지 설정하고
캐릭터가 자주 부활하거나, 캐릭터가 변경되는 게임
	컨트롤러에서 입력 바인딩
캐릭터가 고정이면 캐릭텅 넣는것도 나쁘지 않음

바인드는 포제스 언 포제스 함수

- 서브시스템?

setupAttachement : 생성자에서는 반드시 이거 사용
AttachToComponent : 게임 중간에 붙일 때

## 기타 팁
- 부모의 begin play는 반드시 실행하자