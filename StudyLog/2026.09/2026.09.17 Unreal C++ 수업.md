
# 1. 지난시간 실습

```cpp file:StatAttributeSet
void UStatAttributeSet::PostGameplayEffectExecute(const FGameplayEffectModCallbackData& Data)
{
	Super::PostGameplayEffectExecute(Data);

	//if (Data.EvaluatedData.Attribute == GetHealthAttribute())
	//{
	//	// 이팩트로 인해 변경된 어트리뷰트가 Health다
	//}

	if (Data.EvaluatedData.Attribute == GetDamageAttribute())
	{
		const float LocalDamage = GetDamage();
		SetDamage(0.0f);

		if (LocalDamage > 0)
		{
			float FinalDamage = LocalDamage - GetArmor();	// 각 종 계산 추가(방어력, 최소대미지보장, 쉴드, 피해 증가 등등)
			FinalDamage = FMath::Max(1.0f, FinalDamage);

			const float NewHealth = FMath::Clamp(GetHealth() - FinalDamage, 0.0f, GetMaxHealth());
			SetHealth(NewHealth);

			//값에 따른 추가 처리 
		}
	}
	else if (Data.EvaluatedData.Attribute == GetStaminaCostAttribute())
	{
		const float LocalCost = GetStaminaCost();
		SetStaminaCost(0.0f);

		if (LocalCost > 0)
		{
			float FinalCost = LocalCost;	// 각 종 계산 추가(방어력, 최소대미지보장, 쉴드, 피해 증가 등등)
			FinalCost = FMath::Max(1.0f, FinalCost);

			const float NewStamina = FMath::Clamp(GetStamina() - FinalCost, 0.0f, GetMaxStamina());
			SetStamina(NewStamina);

			//값에 따른 추가 처리
		}
	}
}

void UStatAttributeSet::AdjustAttributeForMaxChange(float InOldValue, float InNewMaxValue, const FGameplayAttribute& AffectedAttributeProperty)
{
	UAbilitySystemComponent* ASC = GetOwningAbilitySystemComponent();
	if (!ASC) return;

	float Delta = InNewMaxValue - InOldValue;
	if (Delta > 0)
	{
		ASC->ApplyModToAttributeUnsafe(AffectedAttributeProperty, EGameplayModOp::Additive, Delta);
	}
	else
	{
		bool bFound = false;
		const float Current = ASC->GetGameplayAttributeValue(AffectedAttributeProperty, bFound);
		if (InNewMaxValue < Current)
		{
			Delta = InNewMaxValue - Current;
			ASC->ApplyModToAttributeUnsafe(AffectedAttributeProperty, EGameplayModOp::Additive, Delta);
		}
	}

}
```

</br>

# 2. Effect 탐구하기
## 2.1 커브 테이블
![[2026.09.17 Unreal C++ 수업-1789638970745.webp]]

![[2026.09.17 Unreal C++ 수업-1789639016114.webp]]
- 리니어
	- 유체화 같은거 구현할 때 좋은 친구
	![[2026.09.17 Unreal C++ 수업-1789639056714.webp]]

- 컨스턴트
	- 레벨당 기본 공격력 같은거거
	![[2026.09.17 Unreal C++ 수업-1789639086639.webp]]

- 큐빅

- 컴포짓
	- 여러 커브 테이블을 하나로 합칠 수 있음
	- 동일한 커브는 나중에 들어온 값을 덮어씌움
	![[2026.09.17 Unreal C++ 수업-1789639159432.webp]]
## 2.2 Duration
 - Periodic Inhibition Policy
	 - 태그, 요구 조건 등의 이유로 Periodic GE Inhibited 상태가 되었다가 해제될 때
		 - NeverReset : 그냥 하던대로
		 - ResetPeriod : 해제 시점부터 다시 이펙트 시작
		 - Execute and Reset Period : 받는 시점에 데미지 주고 다시 시작
	 ![[2026.09.17 Unreal C++ 수업-1789639399079.webp]]


 - Max Duration Magnitude
	 - GE가 가질 수 있는 최대 지속시간
## 2.3 Component
GE의 적용 조건을 검사하는 모든 Component가 허용해야 GameplayEffect 전체가 적용된다. 하나라도 `CanGameplayEffectApply()`에서 `false`를 반환하면 GE 적용이 차단된다.
- Immunity
	- 어떤 대상을 변경하는 이펙트를 무시할 수 있음(면역)
	- 아래의 경우 Damage를 변경하는 이펙트를 무시함
	![[2026.09.17 Unreal C++ 수업-1789640112348.webp]]

- Chance to apply effect
	- 해당 게임 이펙트 전체가 대상에게 적용될 확률
	![[2026.09.17 Unreal C++ 수업-1789640223156.webp]]


## 2.4 Modifier
- Attribute Based
	- 특정 어트리뷰트를 기반으로 타겟 어트리뷰트 변경
	- 아래의 예시는 공격력 어트리뷰트의 1.5배를 Damage 어트리뷰트에 적용
	- Snapshot을 켜는 경우, 해당 이펙트가 생성되는 시점의 값을 캡쳐쳐
		![[2026.09.17 Unreal C++ 수업-1789640575081.webp]]

- Custom Calculate
	- 별도의 클래스를 만들어서 Magnitude 산출
	- 아래의 코드처럼 사용하는게 국룰
	![[2026.09.17 Unreal C++ 수업-1789640852406.webp]]

	```cpp file:MMC_Damage.cpp
	#include "GAS/MMC/MMC_Damage.h"
#include "GAS/StatAttributeSet.h"
#include "GameplayEffectExecutionCalculation.h"

//-----------------------------------------------------
struct FDamageStatics
{
	DECLARE_ATTRIBUTE_CAPTUREDEF(AttackPower);
	DECLARE_ATTRIBUTE_CAPTUREDEF(Armor);
	FDamageStatics()
	{
		// 소스의 공격력을 생성시점 기준 (snap==true)로 캡쳐
		DEFINE_ATTRIBUTE_CAPTUREDEF(UStatAttributeSet, AttackPower, Source, true);

		// 대상의 방어력을 피격시점기준 (snap==false)로 캡쳐
		DEFINE_ATTRIBUTE_CAPTUREDEF(UStatAttributeSet, Armor, Target, false);
	}
};

static const FDamageStatics& DamageStatics()
{
	static FDamageStatics DStatics;
	return DStatics;
}
//-----------------------------------------------------

UMMC_Damage::UMMC_Damage()
{
	// 어떤 어트리뷰트를 캡쳐할 것인지 지정
	RelevantAttributesToCapture.Add(DamageStatics().AttackPowerDef);
	RelevantAttributesToCapture.Add(DamageStatics().ArmorDef);
}

float UMMC_Damage::CalculateBaseMagnitude_Implementation(const FGameplayEffectSpec & Spec) const
{
	FAggregatorEvaluateParameters EvalParams;

	float Attack = 0.0f;
	GetCapturedAttributeMagnitude(DamageStatics().AttackPowerDef, Spec, EvalParams, Attack);

	float Defence = 0.0f;
	GetCapturedAttributeMagnitude(DamageStatics().ArmorDef, Spec, EvalParams, Defence);

	UE_LOG(LogTemp, Display, TEXT("%f, %f"), Attack, Defence);

	float Damage = FMath::Max(1.0f, Attack - Defence);

	return Damage;
}
	```


- SetByCaller
	- GE에서 지정한 태그를 키로 사용하여 Magnitude 주입
![[2026.09.17 Unreal C++ 수업-1789641048333.webp]]

```cpp title:SetByCaller_예저
void ATest02_3::ApplyGameplayEffect()
{
	if (!Target) return;
	if (!GameplayEffectClass) return;
	if (!ASC) return;

	UAbilitySystemComponent* TargetASC = Target->GetAbilitySystemComponent();
	if (!TargetASC) return;

	// 컨택스트 설정(이펙트의 정보들을 설정)
	FGameplayEffectContextHandle EffectContext = ASC->MakeEffectContext();
	EffectContext.AddSourceObject(this);

	// 이팩트 스팩 설정
	FGameplayEffectSpecHandle SpecHandle = ASC->MakeOutgoingSpec(GameplayEffectClass, EffectLevel, EffectContext);
	if (!SpecHandle.IsValid()) return;

	// 테그 확인
	if (!SetByCallerTag.IsValid())
	{
		SetByCallerTag = FGameplayTag::RequestGameplayTag(FName("GAS.Test.SetByCaller"), false);
	}
	// SetByCaller를 이용해 값 주입
	if (SetByCallerTag.IsValid())
	{
		SpecHandle.Data->SetSetByCallerMagnitude(SetByCallerTag, SetByCallerMagnitude);
	}

	//Source ASC가 TargetASC에 스펙을 적용
	FActiveGameplayEffectHandle ActiveEffectHandle = ASC->ApplyGameplayEffectSpecToTarget(*SpecHandle.Data.Get(), TargetASC);



}
```



</br>


# 3. GamePlayTag

계층적으로 사용가능한 태그, 나중에 배우면 엄청 유용할 것이라고 함.
프로젝트 세팅 -> GameplayTag -> Add new Gameplay tag source를 통해서 새로운 소스 이름.ini 파일을 만들어 구분해서 사용가능
Manage Gameplay Tags에서 소스를 선택하고 X.Y.Z형태로 태그를 추가 가능함.
![[2026.09.17 Unreal C++ 수업-1789641227231.webp]]

![[2026.09.17 Unreal C++ 수업-1789641401390.webp]]

</br>

# 4. 미세 팁
`GetInstigator()`는 인스티게이터를 지정해주지 않으면 nullptr 반환
# 4. 학습할 키워드
- [ ] 

