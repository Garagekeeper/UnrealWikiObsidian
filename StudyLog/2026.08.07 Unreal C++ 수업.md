
# 1.  데이터 테이블을 이용한 아이템 스폰
## 데이터 테이블을 불러와  사용하기
데이터 테이블을 사용하기 위한 함수로는`GetRowMap()`과 `GetAllRows()`가 있음
`GetRowMap()`은 함수의 이름에서 유추할 수 있듯 Map을 반환 Value가 uint8* 타입이라 `reinterpret_cast`를 사용해서 원하는 타입으로 변경해서 사용한다.
`GetAllRows()`은 다음과 같은 시그니쳐를 가진다
```cpp
void GetAllRows(const FString& ContextString, OUT TArray<T*>& OutRowArray) const
```
여기서 ContextString은 해당 함수 동작 실패시 출력할 문자열이고, OutRowArray는 Row들을 담을 배열의 참조이다.
## 데이터 테이블을 통해서 스폰하기
`SpawnActor<T>(Uclass* Class)` 이 함수의 반환값은 T타입 일텐데, 원하는 타입으로 다운 캐스팅 할 수 있을까나 
- 가능하다! 애초에 포인터 타입만 T* 일 뿐, 매개 변수로 들어간 클래스의 타입으로 선언된 것이기 때문에 타입을 알고 있다면 다운 캐스팅이 가능하다

# 2. 범용적인 데이터 에셋 만들기
여러 데이터 에셋들의 공통점을 추출하는 추상화의 한 과정으로 보면 된다. 각 에셋들이 공통으로 가질 속성을 뽑아 기저 클래스로 만들고 자신만의 요소는 파생 클래스에서 정의한다.

```cpp file:ItemDataAsset.h hlt:6
#include "CoreMinimal.h"
#include "Engine/DataAsset.h"
#include "Engine/StreamableManager.h"
#include "Item/PickupBase.h"
#include "ItemDataAsset.generated.h"
// 모든 pickup item의 기저클래스

UCLASS(Abstract, BlueprintType)
class UNREAL_CPP_API UItemDataAsset : public UPrimaryDataAsset
{
	GENERATED_BODY()

public:
	TSharedPtr<FStreamableHandle> RequestDataLoad(FStreamableDelegate InDelegate) const;
	virtual bool IsLoaded() const;

protected:
	virtual void OnAsyncRequest(TArray<FSoftObjectPath>& InOutArray) const;

public:
	// 이름, 가격, 오프셋, 소환할 액터 등 공통적인 요소를 묶는다.
	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category = "Base|Data")
	FText DisplayName;

	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category = "Base|Data")
	FText Description;

	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category = "Base|Data")
	int32 Price = 1;

	// 스폰시의 오프셋
	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category = "Base|Spawn")
	FVector SpwanLocationOffset = FVector::ZeroVector;

	UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Base|Spawn")
	TSoftClassPtr<APickupBase> PickUpActor;
};
```

```cpp file:ItemDataAsset.cpp hlt:1
// 기본 클래스에서 데이터 로딩과 관련한 기능들을 만들어 놓고 자식 클래스에서 이것을 활용함
//로딩을 요청하는 함수
TSharedPtr<FStreamableHandle> UItemDataAsset::RequestDataLoad(FStreamableDelegate InDelegate) const
{
	TArray<FSoftObjectPath> TargetsToLoad;
	OnAsyncRequest(TargetsToLoad);

	FStreamableManager& Streamable = UAssetManager::GetStreamableManager();
	return Streamable.RequestAsyncLoad(TargetsToLoad, MoveTemp(InDelegate));
}

// 로딩 되었는지 확인하는 함수
bool UItemDataAsset::IsLoaded() const
{
	return PickUpActor.IsValid();
}

// 로딩할 목록을 채우는 함수
void UItemDataAsset::OnAsyncRequest(TArray<FSoftObjectPath>& InOutArray) const
{
	InOutArray.Add(PickUpActor.ToSoftObjectPath());

	// 상속받은 클래스에서 추가 요소들 추가 등록
}
```

```cpp file:MiscItemDataAsset.h hlt:5-6
#include "CoreMinimal.h"
#include "Data/Item/ItemDataAsset.h"
#include "MiscItemDataAsset.generated.h"

// UItemDataAsset을 상속 받아서 공통적인 속성은 이미 가지고 있다.
// 거기에 자신만이 가지는 속성을 추가로 선언한다.
UCLASS()
class UNREAL_CPP_API UMiscItemDataAsset : public UItemDataAsset
{
	GENERATED_BODY()
public:
	virtual bool IsLoaded() const;

protected:
	virtual void OnAsyncRequest(TArray<FSoftObjectPath>& InOutArray) const;

public:
	// Mesh Of Item
	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category = "Misc|Appearance")
	TSoftObjectPtr<UStaticMesh> Mesh;

	
};
```


<br>
데이터 에셋이 준비 되었으면 이를 들고있을 드랍테이블도 수정해야한다.

```cpp file:ItemDropTable.h hlt:13,15
#pragma once

#include "CoreMinimal.h"
#include "Engine/DataTable.h"
#include "Data/WeaponDataAsset.h"
#include "ItemDropTable.generated.h"

USTRUCT(BlueprintType)
struct FItemDropTableRow : public FTableRowBase
{
	GENERATED_BODY()

	// 앞에서 추상화한 에셋으로 타입 변환
	UPROPERTY(EditAnywhere, BlueprintReadOnly)
	TObjectPtr<UItemDataAsset> DropItemAsset;

	UPROPERTY(EditAnywhere, BlueprintReadOnly, meta = (ClampMin = "0", ClampMax = "1"))
	float DropRate = 0.5f;
};
```


<br>


데이터 테이블을 수정하고 세팅까지 잘 완료했다면 이제는 테이블을 불러와서 사용할 차례이다.
```cpp file:EnemyCharacter.cpp
void AEnemyCharacter::DropItem()
{
	if (!IsValid(ItemDropTable)) return;

	float RandNum = FMath::FRandRange(0.0, 1.0);
	for (const auto& [Key, Value] : ItemDropTable->GetRowMap())
	{
		FItemDropTableRow* DropTableRow = reinterpret_cast<FItemDropTableRow*>(Value);
		if (!DropTableRow) continue;
		float currentItemSpawnRate = DropTableRow->DropRate;
		if (currentItemSpawnRate >= RandNum) continue;


		UItemDataAsset* PickupData = DropTableRow->DropItemAsset;

		UGameInstance* GameInstance = GetGameInstance();
		if (!GameInstance) return;
		UPickupFactorySubsystem* SubSystem = GameInstance->GetSubsystem<UPickupFactorySubsystem>();

		FActorSpawnParameters SpawnParam;
		SpawnParam.Owner = nullptr;
		SpawnParam.ObjectFlags = RF_Transient;
		FTransform NewTransform = GetTransform();
		FVector NewLocation = NewTransform.GetLocation();
		NewLocation.X += FMath::FRandRange(-100.0, 100.0);
		NewLocation.Y += FMath::FRandRange(-100.0, 100.0);
		NewTransform.SetLocation(NewLocation);
		
		if (!PickupData->IsLoaded())
		{
			PickupData->RequestDataLoad(
				FStreamableDelegate::CreateWeakLambda(
					this,
					[this, SubSystem, PickupData, NewTransform]()
					{
						SubSystem->SpawnPickup(PickupData, NewTransform);
					}
				)
			);
		}
		else
		{
			SubSystem->SpawnPickup(PickupData, NewTransform);
		}

	}
}
```

# 3 기타 팁

# 4. 학습할 키워드
- [x] [[SpawnActor()]]
