# 1.  데미지 팝업 띄우기
## WBP 생성
![[2026.08.04 Unreal C++ 수업-1785802446061.webp]]
Widget Componet의 Render option에서 Screen과 World의 차이

단순히 컴포넌트를 부착시켜서 하면 다단히트 데미지가 올바르게 처리되지 않는다. 컴포넌트 부착 없이 소환하는 방식으로 가자.
데미지 팝업만 가진 액터를 만들고, 데미지를 받으면 액터를 소환, begin play에서 popup anim을 재생한다. 그런데 데미지는 어떻게 넘길까? -> 호출시 노출, 인스턴스에서 편집가능을 켜 놓으면 만들어질 때 부터 해당 변수를 수정할 수있음

![[2026.08.04 Unreal C++ 수업-1785805206665.webp]]
![[2026.08.04 Unreal C++ 수업-1785805215154.webp|820x391]]
이 과정을 코드로 진행해봅시다.

```cpp file:DamagePopupWidget.h
 #include "CoreMinimal.h"
#include "Blueprint/UserWidget.h"
#include "DamagePopupWidget.generated.h"


class UTextBlock;

/**
 * 
 */
UCLASS()
class UNREAL_CPP_API UDamagePopupWidget : public UUserWidget
{
	GENERATED_BODY()
	
	
public:
	UFUNCTION(BlueprintCallable)
	void SetDamage(float InDamage);

	UFUNCTION(BlueprintCallable)
	void PlayPopupAnimation();

protected:
	UPROPERTY(VisibleDefaultsOnly, meta = (BindWidget))
	TObjectPtr<UTextBlock> DamageText;

	UPROPERTY(Transient, meta = (BindWidget))
	TObjectPtr<UWidgetAnimation> PopupAnimation;
	
};
```

```cpp file:DamagePopupWidget.cpp
#include "Widget/DamagePopupWidget.h"
#include "Components/TextBlock.h"

void UDamagePopupWidget::SetDamage(float InDamage)
{
	DamageText->SetText(FText::AsNumber(static_cast<int32>(InDamage)));
}

void UDamagePopupWidget::PlayPopupAnimation()
{
	PlayAnimation(PopupAnimation);
}

```


# 2. 오브젝트 풀링
풀에 가용한 오브젝트가 없으면
- 리턴 안하기
- 생성하기
- 재활용하기

게임 인스턴스에서 풀을 관리하고 레벨이 바뀌면 리셋되는 방향으로 설계한다. GI, GM에 풀을 계속 늘리면 클래스가 비대해지고 이 현상은 별로 권장되지 않는다. 이번 수업에서는 서브시스템을 도입해서 오브젝트 풀링을 담당한다. 
풀에의해 관리될 액터들은 초기화 함수가 있어야하고 반납을 구현해야한다.

### Developer Settings
이 세팅? 모듈?을 통해서 오브젝트 풀링을 적용할 타입을 저장할 수 있다.
```cpp file:ObjectPoolSettings.h


#include "CoreMinimal.h"
#include "Engine/DeveloperSettings.h"
#include "ObjectPoolSettings.generated.h"

/**
 * 
 */

// Config = Game : 설정창의 Game 카테고리에 저장 (congif/DeafultGame.ini)
// DefaultConfig : 이 클래스 값이 기본 설정 (프로젝트 설정 원본 = Config 폴더의 ini에 저장)

UCLASS(Config = Game, DefaultConfig, meta = (DisplayName = "Object Pool Settings"))
class UNREAL_CPP_API UObjectPoolSettings : public UDeveloperSettings
{
	GENERATED_BODY()

public:
	// Config : 이 프로퍼티는 ini파일에 저장되고 불러와질 수 있다.
	// TSoftClassPtr : 타입에 대한 소프트 레퍼런스
	UPROPERTY(Config, EditAnyWhere, Category = "Pool Type")
	TSoftClassPtr<AActor> DamagePopupClass = nullptr;
	
};

```

위 코드에 의해서 다음과 같은 설정창 생성
![[2026.08.04 Unreal C++ 수업-1785810289318.webp]]


## 서브 시스템 만들기
서브시스템은 만드는 순간 들어간다. (만드는 순간 적용된다는 말인듯)

```cpp file:ObjectPoolSubSystem.h

#include "CoreMinimal.h"
#include "Subsystems/GameInstanceSubsystem.h"
#include "ObjectPoolSubsystem.generated.h"

/**
 * 
 */
 
// 하나의 풀을 담당하는 구조체 
USTRUCT()
struct FActorPool
{
	GENERATED_BODY()

	UPROPERTY(Transient)
	TArray<TObjectPtr<AActor>> ReadyActors;

	// 실제 사용 중인 액터들
	UPROPERTY(Transient)
	TSet<TObjectPtr<AActor>> ActiveActors;
};

UCLASS()
class UNREAL_CPP_API UObjectPoolSubsystem : public UGameInstanceSubsystem
{
	GENERATED_BODY()
	
public:
	// 풀에서 액터를 가져오는 함수
	AActor* Spawn(AActor* InType, const FTransform& InTransForm);
	// 풀에 액터를 반납하는 함수
	void ReturnPool(AActor* InActor);

	// 서브시스템 초기화 함수
	virtual void Initialize(FSubsystemCollectionBase& Collection) override;
	// 특정 타입의 풀을 생성하는 함수
	void CreatePool(TSoftClassPtr<AActor> InActor);

protected:
	// 풀을 관리하는 해시 맵
	UPROPERTY(Transient)
	TMap<UClass*, FActorPool> PoolMap;

	// 풀 사용시 실제 생성될 클래스
	UPROPERTY(Transient)
	TMap<UClass*, TSubclassOf<AActor>> PoolableClass;

protected:

```

```cpp file:ObjectPoolSubSystem.cpp
#include "Framework/SubSystem/ObjectPoolSubsystem.h"
#include "Config/ObjectPoolSettings.h"
#include "Interface/Poolable.h"

AActor* UObjectPoolSubsystem::Spawn(AActor* InType, const FTransform& InTransForm)
{
	if (!IsValid(InType)) return nullptr;
	if (!GetWorld()) return nullptr;
	// 해당 액터가 클래스에 풀 맵에 없는 경우
	if (!PoolMap.Find(InType->GetClass()))
	{
		UE_LOG(LogTemp, Log, TEXT("This class can not ppoolable"));
		return nullptr;
	}

	// 해당 클래스의 UClass를 가져옴
	UClass* Target = InType->GetClass();
	AActor* Spawned = nullptr;


	// 해당 타입의 풀에서 사용 가능한 액터가 있으면
	if (PoolMap[Target].ReadyActors.Num() > 0)
	{
		// 풀에서 가져오기
		Spawned = PoolMap[Target].ReadyActors.Pop();
		Spawned->SetActorTransform(InTransForm);
	}
	// 해당 타입의 풀에 사용 가능한 액터가 없으면
	else
	{
		// 해당 타입이 풀링이 가능한 액터인지 확인
		if (PoolableClass.Contains(Target) && GetWorld())
		{
			// 생성을 위한 파라미터 설정
			FActorSpawnParameters SpawnParam;
			SpawnParam.Owner = nullptr;
			SpawnParam.ObjectFlags = RF_Transient;

			// 실제로 액터 소환
			Spawned = GetWorld()->SpawnActor<AActor>(PoolableClass[Target], InTransForm, SpawnParam);

#if WITH_EDITOR
			// 소환 성공시 아웃라이너의 Pool 폴더에 넣는다
			if (Spawned)
			{
				Spawned->SetFolderPath(FName("Pool"));
			}
#endif
		}
	}

	// 소환 성공시
	if (Spawned)
	{
		// Poolabel 인터페이스를 상속했다면
		if (Spawned->GetClass()->ImplementsInterface(UPoolable::StaticClass()))
		{
			// Spawn시 실행될 함수 실행
			IPoolable::Execute_Onspawn(Spawned);
			// 현재 소환된 액터에 기록록
			PoolMap[Target].ActiveActors.Add(Spawned);
		}
	}

	return Spawned;
}

void UObjectPoolSubsystem::ReturnPool(AActor* InActor)
{
	if (InActor)
	{

		// Poolabel 인터페이스를 상속했다면
		if (InActor->GetClass()->ImplementsInterface(UPoolable::StaticClass()))
		{
			// 반납시 실행될 함수 실행
			IPoolable::Execute_OnReturn(InActor);
		}

		// 대상의 타입을 가져옴
		UClass* Target = InActor->GetClass();
		
		// 사용중인 액터 목록에서 지우고, 풀에 넣는다.
		PoolMap[Target].ActiveActors.Remove(InActor);
		PoolMap[Target].ReadyActors.Add(InActor);
	}
}

void UObjectPoolSubsystem::Initialize(FSubsystemCollectionBase & Collection)
{
	Super::Initialize(Collection);

	const UObjectPoolSettings* Settings = GetDefault<UObjectPoolSettings>();
	if (Settings)
	{
		//if (Settings->DamagePopupClass.IsNull())
		//{
		//	DamagePopupClass = Settings->DamagePopupClass.LoadSynchronous();
		//}

		for (TSoftClassPtr<AActor> PClass : Settings->PoolableClass)
		{
			CreatePool(PClass);
		}
	}
}

void UObjectPoolSubsystem::CreatePool(TSoftClassPtr<AActor> InActor)
{
	UClass* InClass = InActor.Get();
	if (!PoolMap.Find(InClass))
	{
		
		PoolableClass.FindOrAdd(InClass, InActor.LoadSynchronous());
		PoolMap.FindOrAdd(InClass, FActorPool());
	}
}
```

## 실제 사용 예시
```cpp file:DummyActor.cpp
	if (UGameInstance* GameInstance = GetGameInstance())
	{   
		// 서브시스템 호출 방법
		// 게임 인스턴스 에서 원하는 타입의 서브시스템을 가져온다
		UObjectPoolSubsystem* SubSystem = GameInstance->GetSubsystem<UObjectPoolSubsystem>();
		
		// UObjectPoolSubsystem을 통해서 액터 소환
		AActor* SpawnedActor = SubSystem->Spawn(DamagePopup->GetDefaultObject<AActor>(),PopupTransform->GetComponentTransform());
		
		// 소환된 액터 사용
		ADamagepopupActor* Popup = Cast<ADamagepopupActor>(SpawnedActor);
		if(Popup)
			Popup->OnPopupStart(Damage);
			
	}
	
```

# 4. 기타 팁
