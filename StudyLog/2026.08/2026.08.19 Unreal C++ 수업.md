
# 1.  지난 실습 풀이
## UI바인딩
실습 풀이에서는 외부 의존성 주입으로 해결. 내부 변수로 인벤토리를 들고있음
```cpp file:InventoryWidget.cpp
#pragma once

#include "CoreMinimal.h"
#include "Blueprint/UserWidget.h"
#include "InventoryWidget.generated.h"

class UButton;
class UMoneyPanelWidget;
class UUniformGridPanel;
class UInventoryComponent;

/**
 * 
 */
UCLASS()
class UNREAL_CPP_API UInventoryWidget : public UUserWidget
{
	GENERATED_BODY()

protected:
	UPROPERTY(VisibleDefaultsOnly ,BlueprintReadOnly, meta=(Bindwidget))
	TObjectPtr<UButton> CloseBtn;

	UPROPERTY(VisibleDefaultsOnly ,BlueprintReadOnly, meta=(Bindwidget))
	TObjectPtr<UMoneyPanelWidget> MoneyPanel;

	UPROPERTY(VisibleDefaultsOnly ,BlueprintReadOnly, meta=(Bindwidget))
	TObjectPtr<UUniformGridPanel> SlotGridPannel;

private:
	UPROPERTY(VisibleDefaultsOnly, BlueprintReadOnly, meta = (AllowprivateAccess = "true"))
	TWeakObjectPtr<UInventoryComponent> TargetInventory;
	
};
```



# 3. 기타 팁
```cpp
// 이렇게 간단하게 투명 처리 가능
Icon->SetBrushTintColor(FLinearColor::Transparent);
```

- USTRUCT는 UObject가 아니기때문에 TWeakObjectPtr, TObjectPtr에 사용할 수 없다.

- Controller가 '나'고 나머지들이 컨트롤러에 붙어있는것

- UI는 디버깅이 힘들어잉 그래서 디버그에 위젯 리플렉터를 사용하자 
- 

![[2026.08.19 Unreal C++ 수업-1787121561938.webp]]
위 사진에서 빨간 선으로 표시해둔 `Pick Hit-Testable Widets`을 선택하면 현재 에디터에 있는 위젯 중에서 Hit Test가 가능한 위젯을 선택할 수 있게된다.

![[2026.08.19 Unreal C++ 수업-1787121653981.webp]]
위 사진은 빈 인벤토리의 첫번째 슬롯을 선택(클릭)한 사진으로 Reflector를 살펴보면 내가 선택한 위젯의 속성을 볼 수 있다. 이 상태에서 마우스를 다른 곳으로 움직이면 Refelctor의 focus가 다른 곳으로 넘어가기 때문에, ESC를 눌러 현재 Piking (선택을 확정)해주자.

![[2026.08.19 Unreal C++ 수업-1787121707758.webp]]
피킹이 되어싿면 이제 원하는 속성을 살펴보며 디버깅하자.

# 4. 학습할 키워드
- [x] [[USTRUCT]]
