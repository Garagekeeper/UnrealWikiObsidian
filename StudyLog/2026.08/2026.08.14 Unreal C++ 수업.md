
# 1.  인벤토리 컴포넌트 cpp 채우기
[[check(expreesion)]] -> assert, shipping에 포함 x
[[verify(expression)]]  -> assert, shipping에 포함
[[ensure(expression)]] -> 로그 출력 후 계속, shipping에 포함

# 2. Command Pattern

```cpp 
#pragma once

#include "CoreMinimal.h"
#include "InventoryCommandTypes.generated.h"

class UItemDataAsset;

UENUM(BlueprintType)
enum class EInventoryCommandType :uint8
{
	None,
	Add,
};

USTRUCT(BlueprintType)
struct UNREAL_CPP_API FInventoryCommand
{

	GENERATED_BODY()

public:
	UPROPERTY(BlueprintReadWrite, Category = "Inventory|Command")
	EInventoryCommandType Type = EInventoryCommandType::Add;

	UPROPERTY(BlueprintReadWrite, Category = "Inventory|Command")
	int32 Count = 1;

	UPROPERTY(BlueprintReadWrite, Category = "Inventory|Command")
	TObjectPtr<UItemDataAsset> ItemData;

	UPROPERTY(BlueprintReadWrite, Category = "Inventory|Command")
	int32 SourceIndex = 1;

	UPROPERTY(BlueprintReadWrite, Category = "Inventory|Command")
	int32 TargetIndex = 1;

	UPROPERTY(BlueprintReadWrite, Category = "Inventory|Command")
	FVector2D ScreenPostion = FVector2D::ZeroVector;

public:
	// 헬퍼 함수
	static FInventoryCommand MakeAdd(UItemDataAsset* InItemData, int32 InCount = 1)
	{
		FInventoryCommand Command;
		Command.Type = EInventoryCommandType::Add;
		Command.ItemData = InItemData;
		Command.Count = InCount;
		return Command;
	}
};

```

```cpp file:InventoryComponent.cpp
	// 커맨드 실행용 함수
	UFUNCTION(BlueprintCallable, Category = "Inventory|Command")
	bool ExecuteCommand(const FInventoryCommand& Command);
	
	bool HandleAddCommand(UItemDataAsset* InItemData, int32 InCount);
```

여러 타입의 커맨드에 필요한 변수들? 하나의 구조체에 다 넣거나 파생 구조체로 만들거나
# 3. 기타 팁

# 4. 학습할 키워드
- [x] 
