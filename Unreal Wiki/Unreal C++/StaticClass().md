Uclass* 를 반환하는 함수. 컴파일 타임에 결정되는 클래스의 Uclass를 가져온다. 컴파일 타임에 결정되기 때문에 인스턴스 없이도 호출이 가능하다.
`MyActor::StaticClass()` 형태로 사용가능. 대상 클래스로 고정된 Uclass를 반환한다. 상속을 받던 말던 원본을 반환한다.

클래스의 정보를 미리 알아야할 때(Spawn) wnfh tkdyd