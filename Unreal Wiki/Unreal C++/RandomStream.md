# RandomStream
Native, BP에서 반복적으로 난수를 생성할 수 있는 함수. `RandRange`등 다양한 형태의 난수를 지원 해준다.
`RandomStream.GenerateNewSeed()`을 통해서 새로운 시드를 생성할 수있고`RandomStream.Initialize(InSeed)`를 통해서 원하는 시드를 직접 지정해 줄 수도 있다.
